func solution(s string) bool {
    if len(s) != 4 && len(s) != 6 {
        return false
    }
    
    for _, char := range s {
        if !unicode.IsDigit(char) {
            return false
        }
    }
    
    return true
}
